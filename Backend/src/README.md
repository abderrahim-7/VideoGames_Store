🎮 Game Store Backend

This is the backend for the Game Store project, built with Node.js, Express, and MongoDB.
It provides APIs for managing and retrieving game data.

🚀 Requirements

Node.js (>= 16)

MongoDB (local or Atlas)

npm or yarn

📦 Installation

Clone the repository:

git clone https://github.com/your-username/Video-Game-store.git
cd Backend

Install dependencies:

npm install

Make sure MongoDB is running (either locally or Atlas).

▶️ Running the Server

Start the backend with:

nodemon index.js

The server will run on http://localhost:3000
(default).
